<UserControl x:Class="MVVM_RecipeHandler.Views.AddUnitsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVM_RecipeHandler.Views" xmlns:viewmodels="clr-namespace:MVVM_RecipeHandler.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:UnitAdderViewModel}"
             mc:Ignorable="d" 
             xmlns:converter="clr-namespace:MVVM_RecipeHandler_Models.Converter;assembly=MVVM_RecipeHandler_Models"
             d:DesignHeight="450" d:DesignWidth="800">
   
    <UserControl.Resources>
        <converter:Base64ImageConverter x:Key="BTN_CONV"/>
    </UserControl.Resources>
    
    <StackPanel>
        <Button Height="70" Width="70">
            <Image Source="{Binding Base64String, Converter={StaticResource BTN_CONV}}"/>
        </Button>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBox Text="{Binding NewUnit, UpdateSourceTrigger=PropertyChanged}" Width="100" Margin="3,5,3,5"/>
        </StackPanel>
        <Button Content="Add Unit" Width="80" Command="{Binding AddUnitCommand}"/>
    </StackPanel>
    
</UserControl>
